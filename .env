# Default: Mock Mode (local JSON)
VITE_USE_MOCK=1
VITE_DEMO_LAUNCH=1

# When using SMART Launcher, this is your redirect path
VITE_REDIRECT_PATH=#/

# Replace this when you register a SMART app in the Launcher
VITE_CLIENT_ID=demo-client

# SMART Scopes (for patient-level access)
VITE_SCOPES=launch/patient patient/*.read openid fhirUser online_access